python -m venv venvython -m venv venv
.\venv\Scripts\activate 

pip install --upgrade pip
pip install -r requirements.txt
pip install "fastapi[standard]"

fastapi run

Utils commands:
Save project requirements:

    pip freeze > requirements.txt

