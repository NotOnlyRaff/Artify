apiVersion: v1
kind: ConfigMap
metadata:
  name: pgadmin-servers
data:
  servers.json: |
    {
      "Servers": {
        "1": {
          "Name": "Artify Postgres",
          "Group": "Servers",
          "Host": "db",
          "Port": 5432,
          "MaintenanceDB": "Artify",
          "Username": "postgres",
          "SSLMode": "prefer"
        }
      }
    }